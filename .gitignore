# Config (contains Telegram bot token — excluded from git)
config.yaml

# Secrets — never commit these
credentials.json
.env

# Data files (contain personal portfolio values)
portfolio_data.xlsx
dashboard.html

# Telegram state
.telegram_offset

# Pixi environment
.pixi/
pixi.lock

# Python
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store
